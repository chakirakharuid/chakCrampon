{% extends 'adminBase.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h1>L'utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ utilisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ utilisateur.adresse }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ utilisateur.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a style="margin-left:100px" href="{{ path('app_admin_utilisateurs_index') }}" class="btn btn-dark">Retour à liste</a>

    <a href="{{ path('app_admin_utilisateurs_edit', {'id': utilisateur.id}) }}" class="btn btn-light">Modifier</a>

    {{ include('admin_utilisateurs/_delete_form.html.twig') }}
{% endblock %}
