<form method="post" action="{{ path('app_admin_utilisateurs_delete', {'id': utilisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.id) }}">
    <button  style="margin-left:100px;margin-top:2em"class="btn btn-danger">Supprimer</button>
</form>
