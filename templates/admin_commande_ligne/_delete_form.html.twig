<form method="post" action="{{ path('app_admin_commande_ligne_delete', {'id': commande_ligne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_ligne.id) }}">
    <button style="margin:30px;" class="btn btn-danger">Supprimer</button>
</form>
