<div class="coments">
{{ form_start(form) }}
    {{ form_widget(form) }}
<button style="margin-left:20px" class="btn btn-dark">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
</div>