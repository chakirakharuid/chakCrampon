{% extends 'adminBase.html.twig' %}


{% block title %}Commentaires{% endblock %}

{% block body %}
    <h1>Commentaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ commentaire.Pseudo }}</td>
            </tr>
            <tr>
                <th>Date_commentaire</th>
                <td>{{ commentaire.dateCommentaire ? commentaire.dateCommentaire|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

<a style="margin-left:20px;margin-bottom:25px" class="btn btn-light" href="{{ path('app_admin_commentaires_index') }}">Retour à la liste</a>

    {{ include('admin_commentaires/_delete_form.html.twig') }}
{% endblock %}
