{% extends 'base.html.twig' %}

{% block title %}Commentaires{% endblock %}

{% block body %}
    <h1> Le Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_commentaires_index') }}" class="btn btn-light">Retour à la liste</a>

    <a href="{{ path('app_admin_commentaires_edit', {'id': commentaire.id}) }}" class="btn btn-dark">Modifier</a>

    {{ include('admin_commentaires/_delete_form.html.twig') }}
{% endblock %}
