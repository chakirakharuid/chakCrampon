<form method="post" action="{{ path('app_admin_comandes_delete', {'id': commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id) }}">
    <button style="margin:50px" class="btn btn-danger">Supprimer</button>
</form>
