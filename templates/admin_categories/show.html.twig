{% extends 'adminBase.html.twig' %}

{% block title %}La Catégorie{% endblock %}

{% block body %}
    <h1>La Catégorie</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.nom }}</td>
            </tr>
        </tbody>
    </table>

   <a style="margin-left:30px;" href="{{ path('app_admin_categories_index') }}" class="btn btn-dark">Retour à la liste</a>

   <a style="margin-left:30px;" href="{{ path('app_admin_categories_edit', {'id': category.id}) }}" class="btn btn-light">Modifier</a>


    {{ include('admin_categories/_delete_form.html.twig') }}
{% endblock %}
